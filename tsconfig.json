{
  // 👇 NEW: let Strapi use its server preset
  "extends": "@strapi/typescript-utils/tsconfigs/server",

  "compilerOptions": {
    // ===== your existing options (unchanged) =====
    "module": "CommonJS",
    "moduleResolution": "Node",
    "lib": ["ES2020"],
    "target": "ES2019",
    "strict": false,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "incremental": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "noEmitOnError": true,
    "noImplicitThis": true,
    "outDir": "dist",
    "rootDir": ".",
    "baseUrl": ".",                            // ✅ Kept
    "paths": {
      "types/*": ["types/*"]                   // ✅ Kept
    },

    // 👇 NEW: needed so Strapi + TS play nicely with your JS config
    "allowJs": true,
    "checkJs": false
  },

  "include": [
    // Include root files
    "./",
    // Include all ts files
    "./**/*.ts",
    // Include all js files
    "./**/*.js",
    // Force the JSON files in the src folder to be included
    "src/**/*.json"
  ],

  "exclude": [
    "node_modules/",
    "build/",
    "dist/",
    ".cache/",
    ".tmp/",

    // Do not include admin files in the server compilation
    "src/admin/",
    // Do not include test files
    "**/*.test.*",
    // Do not include plugins in the server compilation
    "src/plugins/**",

    // 👇 NEW: don't re-process generated typings
    "types/generated/**"
  ]
}
